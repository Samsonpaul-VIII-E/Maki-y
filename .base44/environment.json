{
  "previewPort": 3000,
  "healthPath": "/api/health",
  "startCommand": "docker compose -f docker-compose.base44.yml up -d",
  "secrets": [
    {
      "name": "GEMINI_API_KEY",
      "description": "Google Gemini API key for AI features (chat, prompt enhancement, vision analysis). Get it from https://aistudio.google.com/apikey",
      "requiredAtBoot": false,
      "generatedForDevelopment": true
    }
  ]
}
